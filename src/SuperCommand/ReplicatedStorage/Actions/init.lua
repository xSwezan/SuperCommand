return {
	SetVariable = require(script.SetVariable);
	SetArgument = require(script.SetArgument);
	ClearArguments = require(script.ClearArguments);
	SetCurrentArgumentVariable = require(script.SetCurrentArgumentVariable);
}